#define product's name
PRODUCT_NAME=pickering-lxi

# common version
VER_MAJOR=1
VER_MINOR=10
VER_PATCH=3

# version of the header file(s)
HEADER_MAJOR=$(VER_MAJOR)
HEADER_MINOR=$(VER_MINOR)
HEADER_PATCH=$(VER_PATCH)

# version of the library
LIBSO_MAJOR=$(VER_MAJOR)
LIBSO_MINOR=$(VER_MINOR)
LIBSO_PATCH=$(VER_PATCH)

# define versions
VER_PREV=
VER_CURRENT=$(VER_MAJOR).$(VER_MINOR)
VER_NEXT=
